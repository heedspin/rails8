<%= form_with(model: [@product, @product_comment], local: true) do |form| %>
  <% if @product_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product_comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
        <% @product_comment.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :author_name, "Name" %>
    <%= form.text_field :author_name %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.rich_text_area :content, rows: 5 %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>