<h1>Rails 8 Products</h1>

<%= link_to "New product", new_product_path %>

<div id="products">
  <% @products.each do |product| %>
    <h2>
      <%= link_to product.name, product %>
    </h2>
  <% end %>
</div>
